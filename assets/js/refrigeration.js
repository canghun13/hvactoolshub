(()=>{const format=(value,digits=2)=>new Intl.NumberFormat('en-US',{maximumFractionDigits:digits}).format(value);const set=(form,name,value,digits)=>{const output=form.querySelector(`[data-result="${name}"]`);if(output)output.value=Number.isFinite(value)?format(value,digits):''};const clear=(form)=>form.querySelectorAll('[data-result]').forEach(output=>output.value='');const number=(form,name)=>Number(form.elements[name]?.value);const bind=(form,calculate)=>{form.addEventListener('submit',event=>{event.preventDefault();calculate()});form.addEventListener('reset',()=>setTimeout(calculate,0));form.querySelectorAll('input,select').forEach(input=>input.addEventListener(input.tagName==='SELECT'?'change':'input',calculate));calculate()};document.querySelectorAll('[data-refrigeration-form]').forEach(form=>{const type=form.dataset.refrigerationForm,message=form.querySelector('[data-refrigeration-message]');if(type==='capacity'){const unitLabels=()=>{const si=form.elements.unit.value==='si';form.querySelector('[data-mass-unit]').textContent=si?'kg/s':'lb/min';form.querySelector('[data-enthalpy-unit]').textContent=si?'kJ/kg':'BTU/lb';document.querySelector('[data-capacity-formula]').textContent=si?'Capacity (kW) = mass flow (kg/s) × enthalpy difference (kJ/kg)':'Capacity (BTU/h) = 60 × mass flow (lb/min) × enthalpy difference (BTU/lb)'};const calculate=()=>{unitLabels();const mass=number(form,'massFlow'),deltaH=number(form,'enthalpyDelta'),si=form.elements.unit.value==='si';if(!Number.isFinite(mass)||!Number.isFinite(deltaH)||mass<=0||deltaH<=0){clear(form);message.textContent='Enter positive mass-flow and enthalpy-difference values.';return}const kw=si?mass*deltaH:mass*deltaH*60*.2930710702/1000;const btu=kw*3412.141633;set(form,'btu',btu,0);set(form,'kw',kw,3);set(form,'tons',btu/12000,3);message.textContent='Result is the refrigerant-side heat-transfer rate for the stated measurements.'};bind(form,calculate);return}const calculate=()=>{const line=number(form,'lineTemperature'),saturation=number(form,'saturationTemperature'),si=form.elements.unit.value==='si';if(!Number.isFinite(line)||!Number.isFinite(saturation)||line<-100||line>300||saturation<-100||saturation>300){clear(form);message.textContent=si?'Enter temperatures from -100 to 300 °C.':'Enter temperatures from -100 to 300 °F.';return}const delta=type==='superheat'?line-saturation:saturation-line;const kelvinDelta=si?delta:delta*5/9;set(form,'difference',delta,2);set(form,'kelvin',kelvinDelta,2);if(delta<0)message.textContent='The result is negative. Recheck the line temperature, saturation value, measurement location, and refrigerant data.';else if(delta===0)message.textContent='The readings are equal; confirm the measurement locations and operating condition.';else message.textContent='Result is a temperature difference only; it is not a charging or adjustment instruction.'};bind(form,calculate)})})();
